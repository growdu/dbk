[project]
name = "dbk"
version = "0.1.0"
description = "Database Kernel Agent CLI MVP"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages]
find = {}

[project.scripts]
dbk = "dbk.cli:main"
